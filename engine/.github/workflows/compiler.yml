name: Compiler
on: [push, pull_request]
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Compiler Tests
        run: vendor/bin/phpunit --testsuite Compiler
